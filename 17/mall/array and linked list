链表和数组的区别
    数组的优点在于可以直接进行随机访问,缺点是在编译时决定其大小,插入和删除元素很费劲.
    链表的优点在于运行时决定其大小,可以快速的进行插入和删除元素,缺点是不支持随机访问,用户必须提供编程支持.

向数组中插入或删除一个元素,必须移动其他元素以安插新的元素,而向链表中插入一个元素,只需要分
配两个指针值,删除一个元素,只需要重新设置一个指针并释放被删除节点使用的内存即可

访问数据方面:数组随机访问(下标), 链表顺序访问

链表适合频繁的插入和删除元素,数组适合频繁的查找元素,如果两者都需要,采用二叉搜索树